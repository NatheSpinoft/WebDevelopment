
    function changeCSS(style) {
    // Get the link element by ID
    var link = document.getElementById("stylesheet");

    // Change the href attribute to point to the new stylesheet
    link.href = style;
    }
